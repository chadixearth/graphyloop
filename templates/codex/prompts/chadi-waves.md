Use agent-chadi. Call graphyloop_plan_feature with the user's request as the goal. Report the wave table (which lanes run in parallel, what each owns, what gates the deploy), then pass plan.tasks to graphyloop_distribute and dispatch ONLY the tasks it returns in dispatchNow — wave 0 freezes the contract alone. After each result, graphyloop_record it and dispatch whatever that unblocked, all tasks of a wave in ONE tool-call block. Do not let a builder start before the contract file exists.
