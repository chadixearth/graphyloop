Use agent-chadi. Database setup lane. 1) graphyloop_secrets_status to see which Supabase keys exist — never ask the user to paste a key into the chat; if one is missing, tell them the exact key name and where to get it, then store it with graphyloop_secrets_set. 2) graphyloop_env_sync so the app reads real credentials from its own env file. 3) graphyloop_preflight target=db and clear every blocker. 4) Write schema changes as migration files (never dashboard-only edits), with RLS policies and indexes. 5) Run the dry-run step and show its SQL BEFORE any apply; the apply step needs explicit user approval and a rollback note. Never print a credential value.
