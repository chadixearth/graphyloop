Use agent-chadi. Deploy lane. 1) graphyloop_preflight target=deploy — every blocker must be cleared first (missing VERCEL_TOKEN, no build script, env file not git-ignored). 2) Mirror every runtime env key in the Vercel project; a local .env.local is not deployed. 3) Apply pending database migrations before the app deploy, dry-run first, with a rollback note. 4) Run the local build, then a PREVIEW deployment and verify the real URL. 5) Production deploy only after explicit user approval, and record the rollback command in the report. Never print a credential value.
